import { FiltersiswaPipe } from './filtersiswa.pipe';

describe('FiltersiswaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersiswaPipe();
    expect(pipe).toBeTruthy();
  });
});

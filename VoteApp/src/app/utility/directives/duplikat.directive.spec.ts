import { DuplikatDirective } from './duplikat.directive';

describe('DuplikatDirective', () => {
  it('should create an instance', () => {
    const directive = new DuplikatDirective();
    expect(directive).toBeTruthy();
  });
});

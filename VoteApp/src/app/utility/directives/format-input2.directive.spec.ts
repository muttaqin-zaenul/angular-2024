import { FormatInput2Directive } from './format-input2.directive';

describe('FormatInput2Directive', () => {
  it('should create an instance', () => {
    const directive = new FormatInput2Directive();
    expect(directive).toBeTruthy();
  });
});
